local skynet = require "skynet"
local utils = require "utils"

local M = {}

function M.login(msg)
    --local fd = assert(socket.connect(msg.ip, tonumber(msg.port)))
end

function M.register(msg)

end

function M.login_baseapp(msg)

end

return M
